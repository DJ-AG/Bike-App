import { StationContainer, SearchContainer } from "../../components/index";

const allStations = () => {
  return (
    <div>
      <SearchContainer />
      <StationContainer />
    </div>
  );
};

export default allStations;
